package constants

const SELECT = "SELECT "
const INSERT = "INSERT "
const ALL_OPERATOR = " * "
const FROM = " FROM "
const UPDATE = "UPDATE "
const WHERE = " WHERE "
const AND = " AND "
const INTO = " INTO "
const SET = " SET "
const VALUES = " VALUES "

/* Contention rates */
const HIGH = "HIGH"
const LOW = "LOW"